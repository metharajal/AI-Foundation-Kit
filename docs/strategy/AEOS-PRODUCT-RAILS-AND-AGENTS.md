# AEOS Product Rails and Agents

**Date:** 2026-06-29
**Status:** Strategic — Living Document
**Version:** 1.0

---

## Central Phrases

> **Lovable is a use case. Reclaim is a rail. AEOS is the full engineering operating system.**

> **AEOS Core guarantees. AEOS Agents reason. AEOS Memory learns. Humans validate.**

---

## 1. What AEOS Is — and Is Not

### AEOS is not a Lovable clone.

Lovable, Bolt, and Replit are **generation tools**. They produce a working prototype fast.
AEOS is an **engineering operating system**. It creates, governs, reclaims, modernizes, migrates, and operates software — including software generated by Lovable.

The reclaim capability is one rail among nine. It is not the identity of AEOS.

### AEOS covers the full product lifecycle.

| Starting point | AEOS path |
|---|---|
| Nothing — green field | Build Rail |
| AI-generated / no-code project (Lovable, Bolt, Replit) | Reclaim Rail |
| Legacy monolith or inherited system | Modernize Rail |
| Moving from one architecture to another | Migrate Rail |
| Running in production — need to maintain and evolve | Operate Rail |
| Any project — security posture needs verification | Security Rail |
| Any project — external dependency assessment | Sovereignty Rail |
| Cross-cutting task execution with AI | Agents Rail |
| Cross-cutting project knowledge and audit history | Memory Rail |

---

## 2. The Nine Product Rails

### Rail 1 — Build Rail

**Purpose:** Start new projects correctly, with sovereignty and engineering discipline from the first commit.

**AEOS does not generate carelessly.** When AEOS scaffolds a new project, it applies:
- explicit architecture decisions (DB, auth, storage, hosting)
- security gates wired into CI by default
- tests and documentation structure from day one
- portability by design (Dockerfile, docker-compose, .env.example)
- sovereignty by default (no Supabase lock-in, no Vercel lock-in unless explicitly chosen)
- local AI configuration built in

**Commands (Sprint 4A — production-ready):**
```
aeos build plan --name <project> --type <type> --stack <stack>
aeos build plan --name <project> --type <type> --stack <stack> --json
```

Types: `web-app` · `api` · `internal-tool`
Stacks: `nextjs-supabase` · `nextjs-postgres` · `fastapi-postgres` · `generic`

Produces: architecture summary, folder structure, governance files, security
baseline, sovereignty baseline, testing baseline, deployment baseline, next steps.

**Invariants enforced on every Build command:**
- `read_only: true` — no project created, no client file modified
- `applied: false` — no action applied without human gate
- No `.env` file read
- No secret value displayed or logged
- No network access. No AI inference.

**Future commands:**
```
aeos build scaffold   # generate folder structure and governance files
aeos build review     # audit a project under construction
aeos build generate   # generate components with human control
aeos build apply      # apply an action with explicit human gate
```

---

### Rail 2 — Reclaim Rail

**Purpose:** Audit, harden, and recover control of AI-generated, no-code, or low-code projects.

**What it solves:** Lovable, Bolt, and Replit produce working code fast — but without architecture ownership, without portability, without security posture, and with stacks the builder did not consciously choose.

**Current commands (production-ready):**
```
aeos reclaim inspect --path <project>   # read-only control map + exit options
aeos reclaim harden --path <project>    # full cross-domain audit + remediation plan
aeos reclaim harden --path <project> --output <file>  # export Markdown report
```

**Sub-commands:**
```
aeos supabase check --path <project>        # Supabase-specific audit
aeos supabase rls inspect --path <project>  # RLS policy analysis
aeos supabase rls harden --path <project>   # SQL hardening proposal
aeos security check --path <project>        # secret and security scan
aeos sovereignty check --path <project>     # external dependency audit
```

**Invariants enforced on every Reclaim command:**
- `read_only: true` — no client file is modified
- `applied: false` — no migration is applied
- No `.env` file is read
- No secret value is displayed or logged
- No database connection is opened

---

### Rail 3 — Modernize Rail

**Purpose:** Understand, map, and progressively transform monolithic or legacy applications.

**Who it serves:** Enterprises, public institutions, and agencies running systems they cannot fully explain, cannot easily modify, and cannot afford to rewrite.

**AEOS principle:**
> Audit first. Understand before you move. Recommend before you automate.

AEOS does not rewrite the monolith. It gives the team the evidence they need to make that decision with clarity.

**Future commands:**
```
aeos modernize inspect --path ./legacy-app
aeos modernize map --path ./legacy-app
aeos modernize plan --path ./legacy-app
aeos modernize readiness --path ./legacy-app
aeos finops check --path ./legacy-app
```

---

### Rail 4 — Migrate Rail

**Purpose:** Move from one architecture to another — controlled, tested, reversible.

**Migrations supported:**
- Lovable Cloud → own Supabase Cloud
- Lovable Cloud → self-hosted Supabase
- Lovable Cloud → PostgreSQL + open backend
- Firebase → PostgreSQL
- Vercel → VPS / Kubernetes / sovereign cloud
- On-premise monolith → Docker / Kubernetes
- Monolith → modular architecture

**Principles:**
- Branch-based execution — no migration step touches production
- Test-first — every step runs tests before proceeding
- Rollback path — every destructive step has a defined reverse
- Evidence report — every action is logged and auditable

---

### Rail 5 — Operate Rail

**Purpose:** Continuous audit, security, optimization, and documentation of a running product.

**What it provides:**
- Sovereignty and security drift detection
- Periodic audit reports
- AI cost monitoring (local vs. frontier usage)
- Quality gate enforcement in CI
- Feature evolution with local AI

**Commands:**
```
aeos operate check
aeos operate report
aeos evolve --task "..."
```

---

### Rail 6 — Security Rail

**Purpose:** Cross-cutting security verification, always read-only, always non-destructive.

**Scope:**
- Secret detection in Git history (names only, never values)
- Dependency vulnerability scanning
- RLS policy analysis
- Auth configuration review
- Supply chain risk detection

**Invariants:**
- AEOS never displays a secret value
- AEOS never reads `.env` values — only variable names
- AEOS never connects to a live database
- All findings reference `file:line`, never `file:line:value`

**Integration:** Security Rail runs inside Reclaim, Modernize, and Operate rails automatically.

---

### Rail 7 — Sovereignty Rail

**Purpose:** Detect, measure, and reduce external platform dependency.

**What it measures:**
- Generator lock-in (Lovable, Bolt, Replit)
- Provider dependency (Supabase, Firebase, Vercel, Clerk)
- Portability score (Dockerfile, docker-compose, local backend, local schema)
- Control map (frontend, backend, database, auth, storage, deployment)
- Exit options (5 strategic paths, always present)

**Sovereignty levels:**

| Level | Description |
|---|---|
| 1 — Controlled Productivity | Stay on current provider, but secure and documented |
| 2 — Personal Cloud Control | Migrate to own accounts (own Supabase, own Vercel) |
| 3 — Managed Open Source | PostgreSQL, Directus, Keycloak, MinIO |
| 4 — Self-Hosted | All components on controlled infrastructure |
| 5 — Full Sovereign Stack | Zero dependency on foreign platforms |

---

### Rail 8 — Agents Rail

**Purpose:** Cross-cutting task execution with specialized AI agents.

Agents are not a separate product. They are **transversal capabilities** available across all rails.

**See Section 3 for the full agent matrix.**

---

### Rail 9 — Memory Rail

**Purpose:** Local, controlled knowledge accumulation about projects, audits, and engineering decisions.

**What AEOS Memory stores:**
- Audit results and findings history
- Human-validated corrections
- Project-specific rules and invariants
- Dependency maps and portability scores over time
- Migration decisions and their outcomes
- Security incidents and resolutions

**What AEOS Memory does NOT store:**
- Secret values
- `.env` file contents
- Sensitive business data
- Database credentials

**Learning is controlled:**
- Rules are deterministic — they do not emerge from LLM inference alone
- Memory updates require human validation
- No automatic inference from unchecked audit results
- Local storage only — no cloud sync

**The controlled learning loop:**
```
audit result → human review → validated correction → rule update → local memory
test result → human review → pattern validation → rule reinforcement → local memory
```

Memory feeds future audits, not future decisions autonomously.

---

## 3. Specialized Agents — Matrix by Rail

Agents are cross-cutting. Each agent can be called from multiple rails.

| Agent | Primary rail(s) | Role |
|---|---|---|
| Discovery Agent | Reclaim, Modernize | Analyzes repo structure, dependencies, providers, architecture |
| Architecture Agent | Build, Migrate | Proposes target architecture based on current state and constraints |
| Planning Agent | All | Breaks work into safe, reversible, testable steps |
| Database Agent | Reclaim, Migrate | Analyzes schemas, migrations, RLS, exports, imports |
| Backend Agent | Build, Migrate | Migrates or scaffolds APIs, services, edge functions |
| Frontend Agent | Migrate, Modernize | Adapts interface to new backends and APIs |
| Legacy Agent | Modernize | Analyzes monoliths, modules, dependencies, coupling |
| Refactoring Agent | Modernize, Operate | Proposes extraction, modularization, cleanup |
| Security Agent | Security, Reclaim | Verifies secrets, Git history, RLS, permissions — read-only |
| DevOps Agent | Build, Migrate | Scaffolds Dockerfile, docker-compose, CI/CD pipelines |
| Test Agent | All | Generates and runs non-regression tests before each step |
| Evidence Agent | All | Produces proofs, reports, decisions, audit logs |
| Memory Agent | Memory | Stores validated audit results and corrections locally |
| Local AI Orchestrator | All | Routes tasks to local models; decides when frontier is justified |

### Agent governance rules (non-negotiable)

- Every agent reads before it writes.
- No agent takes a destructive action without human approval.
- No agent pushes to production without explicit validation.
- No agent calls frontier AI with `.env` content, secrets, or sensitive business data.
- Every agent action produces an audit log entry.
- Every agent works in an isolated branch, not directly on the main branch.
- No agent updates Memory without a human-validated result.

---

## 4. AEOS Core vs. Agents vs. Memory

```
AEOS Core         — guarantees
AEOS Agents       — reason
AEOS Memory       — learns
Humans            — validate
```

### AEOS Core guarantees

Core commands (`aeos reclaim inspect`, `aeos supabase rls harden`, etc.) enforce:
- `read_only: true`
- `applied: false`
- No secret display
- No `.env` read
- No database connection
- No client file modification

These are code-enforced invariants with tests. They cannot be overridden by agent behavior.

### AEOS Agents reason

Agents apply judgment within the constraints set by Core:
- Which steps to execute
- In which order
- What context is safe to send to frontier AI
- When to stop and ask the human

Agents can make mistakes. That is why human approval exists.

### AEOS Memory learns

Memory accumulates validated knowledge over time:
- Not from LLM inference alone
- From human-confirmed audit results
- From test outcomes
- From explicit engineering decisions

Memory makes future audits faster and more accurate. It does not make future decisions autonomously.

### Humans validate

Human approval is a first-class architectural feature of AEOS, not a fallback:
- Every destructive operation requires explicit confirmation
- Every frontier AI call involving sensitive context requires authorization
- Every Memory update requires a human-validated source
- Every agent action on production infrastructure requires approval

---

## 5. Local AI First — Frontier by Exception

### The routing doctrine

```
Local AI by default.
Frontier AI by exception.
Human approval for sensitive operations.
```

### What local AI handles (free, private, always available)

- Code explanation and documentation
- Unit test generation
- Safe refactoring proposals
- Dependency analysis
- Security finding classification
- Audit report generation
- Planning and task decomposition
- Architecture review

### What frontier AI may handle (explicit, logged, approved)

- Complex multi-file reasoning beyond local model capacity
- Large-context tasks (>32K tokens of relevant context)
- Frontier-class architecture proposals
- Tasks where local model confidence is demonstrably insufficient

### What frontier AI must never receive

- `.env` file contents
- Secret values of any kind (API keys, tokens, passwords)
- Sensitive business data (PII, financial data, healthcare data)
- Database credentials or connection strings
- Migration files containing sensitive schema details (without anonymization)
- Client project data without explicit owner authorization

### Required before any frontier call

1. Context has been stripped of secrets and sensitive data
2. The engineer has explicitly authorized the call
3. Local AI has been confirmed insufficient for the task
4. The output will be verified locally after generation
5. The call is logged in the frontier usage audit

---

## 6. The Build Rail in Detail — AEOS is not a Lovable clone

When AEOS builds a new project, it does not generate carelessly from a single prompt.

**The generation discipline includes:**

| Dimension | What AEOS does | What Lovable does |
|---|---|---|
| Architecture | Explicit, human-chosen, documented | Embedded defaults (Supabase, Vercel) |
| Database | Choice: PostgreSQL, Supabase, MySQL | Supabase by default |
| Auth | Choice: Keycloak, Supabase, Clerk, custom | Supabase Auth by default |
| Storage | Choice: MinIO, Supabase, S3 | Supabase Storage by default |
| Security | Security gate in CI from day one | Not included |
| Tests | Test structure scaffolded, CI wired | Not included |
| Documentation | ADRs, governance directory, .env.example | Minimal |
| Portability | Dockerfile, docker-compose, migration files | Not included |
| Sovereignty | Sovereignty check in CI by default | Not included |
| Local AI | Local model configured in aeos.toml | Not included |
| Lock-in | Explicit and documented if present | Implicit and invisible |

AEOS generates fast. But it generates with discipline.

The difference is not speed. The difference is ownership.

---

## 7. What AEOS Must Not Become

**Not a generation-first tool.**
Speed of generation is a means, not the goal. Ownership, auditability, and portability are the goal.

**Not a one-rail system.**
Reclaim is one rail. AEOS is nine rails. The product must not be reduced to "the tool that fixes Lovable projects."

**Not a tool that silently calls frontier AI.**
Every frontier call is explicit, logged, and approved. Silent escalation is a security failure.

**Not a tool that accumulates dependency.**
AEOS must be deployable without a cloud account, without a SaaS subscription, without a hosted model. The baseline is: local machine, open-weight model, Git.

**Not a tool that makes decisions autonomously.**
Agents propose. Humans decide. Memory records what was validated. The loop is always closed by a human.

**Not a tool that sends secrets to AI.**
`.env` files are never read for their values. Secret values are never logged, displayed, or sent to any model. This is an invariant, not a preference.

**Not a tool that applies changes without a human gate.**
`applied: false` is the invariant of every read-only command. There is no `--force-apply` that bypasses the human review step.

---

## See Also

- [`docs/strategy/AEOS-PRODUCT-VISION.md`](AEOS-PRODUCT-VISION.md) — full strategic vision with use cases, target users, and positioning
- [`docs/features/AEOS-BUILD-RAIL.md`](../features/AEOS-BUILD-RAIL.md) — Build Rail MVP documentation
- [`docs/features/AEOS-RECLAIM-HARDEN.md`](../features/AEOS-RECLAIM-HARDEN.md) — Reclaim Rail harden command documentation
- [`docs/features/AEOS-SUPABASE-RLS-HARDENING.md`](../features/AEOS-SUPABASE-RLS-HARDENING.md) — RLS hardening chain documentation
- [`docs/research/AEOS-RESEARCH-BACKLOG-LOCAL-FIRST-SOVEREIGNTY.md`](../research/AEOS-RESEARCH-BACKLOG-LOCAL-FIRST-SOVEREIGNTY.md) — research backlog on sovereignty and local AI
